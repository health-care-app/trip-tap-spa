export enum ModuleRoutes {
  Root = '',
  Auth = 'auth',
  Customers = 'customers',
}

export enum AuthRoutes {
  Root = '',
  SignIn = 'signin',
  SignUp = 'signup',
}

export enum CustomersRoutes {
  Root = '',
  Trips = 'trips',
  Id = ':id',
}
