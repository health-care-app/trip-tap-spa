{
  "FORM": {
    "ERRORS": {
      "EMAIL": "{{ label }} is not correct email address.",
      "FILE_UPLOAD_ERROR": "There was an error uploading your file: {{ error }}",
      "IS_NOT_VALID": "{{ label }} is not valid.",
      "IS_REQUIRED": "{{ label }} is required",
      "MAX": "{{ label }} must be less than {{ value }}.",
      "MAX_LENGTH": "{{ label }} must be shorter than {{ value }} characters.",
      "MIN": "{{ label }} must be greater than {{ value }}.",
      "MIN_LENGTH": "{{ label }} must be longer than {{ value }} characters.",
      "MUST_BE_INTEGER": "{{ label }} must be integer value.",
      "MUST_BE_SAME": "{{ label }} must be same.",
      "MIN_DURATION": "{{ label }} must be longer than {{ value }}."
    }
  }
}
